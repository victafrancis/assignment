# https://github.com/victafrancis/course-material
language: node_js

node_js:
  - "10.9.0"

cache: npm

before_install:
  - ls -la
  - mkdir dist
  - ls -la

install:
  - ls -la
  - npm install
  - ls -la


after_install:
  # send an email after install completes
  - echo "Sucessfully testing!!!" | mailx -s "Travis Testing" francis.victa@georgebrown.ca
